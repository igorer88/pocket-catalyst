/**
 * Enum representing the different environments the application can run in.
 */
export enum Environment {
  /**
   * Development environment.
   */
  Development = 'development',

  /**
   * Production environment.
   */
  Production = 'production'
}
